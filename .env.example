# ============================================================
# Variáveis de Ambiente - Hardened Next.js
# ============================================================
# Copie este arquivo para .env.local e configure os valores

# URL da API de backend (para Server Actions)
API_URL=https://api.hardened.com.br

# Ambiente
NODE_ENV=development

# Configurações de segurança
# Lista de hosts permitidos para safeFetch (separados por vírgula)
ALLOWED_FETCH_HOSTS=api.hardened.com.br,cdn.hardened.com.br

# Chave secreta para CSRF (gere com: openssl rand -base64 32)
CSRF_SECRET=your-secret-key-here
